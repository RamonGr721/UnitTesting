//Calculator with individual Functions
const calculator = {
    sum: (a, b) => a + b,
    diff: (a, b) => a - b, 
    multiply: (a , b) => a * b,
    divide: (a , b) => a / b
}

module.exports = calculator;